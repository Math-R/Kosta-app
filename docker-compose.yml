version: "3.5"
services:
  front:
    image: node:14
    command: npm run docker
    volumes:
      - ./:/usr/src/app
    working_dir: /usr/src/app
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0

